{%- import '_pieces.md.jinja' as pc -%}
{%- set svg = svg | default('') -%}
{%- set root = root | default('../') -%}
{%- set lang = lang | default('fr') -%}
{{ pc.back(root, lang) }}

# {% if lang == 'en' %}Projects{% else %}Projets{% endif %}

<div align="center">
<a href="https://github.com/{{ profile.username }}?tab=repositories">
{{ pc.fig(root, svg, 'featured-projects.svg', 'Featured public repositories') }}
</a>
</div>

## {% if lang == 'en' %}All public repositories, by domain{% else %}Tous les dépôts publics, par domaine{% endif %}

{% for d in domains | sort(attribute='order') %}
{%- set dp = projects | selectattr('domain', 'equalto', d.id) | list %}
{% if dp %}
**{{ d.label }}**

| {% if lang == 'en' %}Project | Stack | State | Description{% else %}Projet | Stack | État | Description{% endif %} |
|---|---|---|---|
{% for p in dp %}| [{{ p.name }}]({{ p.github_url }}) | {{ p.stack_label | default('—') }} | {{ p.state }} | {{ p.description }} |
{% endfor %}
{% endif %}
{% endfor %}

{{ pc.back(root, lang) }}
